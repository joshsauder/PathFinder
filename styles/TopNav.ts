import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: "row",
        alignItems: "center"
    },
    textMargin: {
        marginLeft: 20,
        alignItems: "center"
    },

    buttonMargin: {
        paddingVertical: 10
    }
})